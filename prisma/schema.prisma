generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = "mysql://0dfs2p4nxf72gfj0z2dg:pscale_pw_yFlbOtfJe6iStcVNb5U0ZkRxJI5Oz09GjMAYDZyAG7v@aws.connect.psdb.cloud/merkle_akeneo_webhook?sslaccept=strict"
  relationMode = "prisma"
}

model events {
  id            Int             @id @default(autoincrement())
  created_at    DateTime        @default(now())
  content       String          @db.Text
  event_details event_details[]
}

model event_details {
  id         Int      @id @default(autoincrement())
  created_at DateTime @default(now())
  event      events   @relation(fields: [event_id], references: [id])
  event_id   Int
  content    String   @db.Text
}
